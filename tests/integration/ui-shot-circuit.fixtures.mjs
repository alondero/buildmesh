// The circuit screenshot exercises queue controls, not provider discovery.
// Leave the shared boot fixture untouched and remove spawn-menu data only for
// this focused smoke route.
export default {
  list_providers: [],
};
