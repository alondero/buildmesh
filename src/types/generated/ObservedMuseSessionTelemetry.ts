// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * Server-computed context pressure. Open on the wire; unknown values map
 * to [`ContextPressureLevel::Unknown`] rather than failing the event.
 */
export type ContextPressureLevel = "normal" | "warning" | "blocked" | "unknown";

/**
 * Context-window occupancy. `pressure` is `used / window` when the host
 * reported a window; the window is omitted, never invented, when absent.
 */
export type ObservedContextUsage = { used_tokens: number, window_tokens: number | null, pressure: number | null, pressure_level: ContextPressureLevel, };

/**
 * Session running totals of counted-once usage. Taken from the MSP
 * `cumulative` block; never re-summed from raw `inputTokens`.
 */
export type ObservedCumulativeUsage = { prompt_tokens: number, output_tokens: number, total_tokens: number, };

/**
 * Public node telemetry payload. Distinct from [`crate::services::usage`]
 * Usage Meters: no remaining quota, reset time, spend, or currency.
 */
export type ObservedMuseSessionTelemetry = { kind: ObservedTelemetryKind, node_id: number, session_id: string, model_id: string | null, last_turn: ObservedTurnUsage | null, cumulative: ObservedCumulativeUsage, context: ObservedContextUsage | null, };

/**
 * Discriminator so consumers can refuse to treat this as a Usage Meter.
 */
export type ObservedTelemetryKind = "observed_session_telemetry";

/**
 * Counted-once per-completion counters plus raw provider cache fields.
 * `prompt_tokens` is the MSP counted-once derivation — not raw
 * `input_tokens`, which may include cache depending on the provider.
 */
export type ObservedTurnUsage = { turn_id: string, prompt_tokens: number, output_tokens: number, total_tokens: number, input_tokens: number, reasoning_tokens: number, cached_tokens: number, cache_read_tokens: number | null, cache_write_tokens: number | null, };
