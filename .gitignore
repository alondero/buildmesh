# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local research files (not for public repo)
user-research-grillme-*.md

# Test artifacts
test-results/
.playwright-cli/

# Stray review/diff artifacts from agent sessions (e.g. `git diff > /tmp/x.patch`
# landing in worktree root on git-bash Windows). Real patch files committed to
# the repo should live in docs/ or similar — pattern is anchored to root only.
/review-diff.patch
/*.patch.tmp

# Claude worktrees and local settings (local only)
.claude/worktrees/
.claude/settings.local.json
.claude/tasks/
.claude/memory/
.claude/scheduled_tasks.lock
.antigravitycli/*.json
CLAUDE.local.md
coverage/
coverage/
.worktrees/
# Git-bash / Cygwin crash dumps (sh.exe.stackdump, bash.exe.stackdump, …)
*.stackdump
